spring:
  application:
    name: gestaodevendas
  datasource:
    url: jdbc:postgresql://localhost:5432/gestaodevendas
    username: ADMIN
    password: ADMIN
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect